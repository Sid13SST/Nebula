/**
 * Tool to capture a screenshot of the active page.
 * @param path The filepath where the screenshot will be saved
 * @returns The resolved absolute path to the saved screenshot file
 */
export async function takeScreenshot(path: string): Promise<string> {
  throw new Error('Not implemented yet');
}
